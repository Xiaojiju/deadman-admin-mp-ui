<page-meta page-style="{{pageStyle}}" />
<view class="page admin-page theme-root {{themeClass}}">
  <scroll-view class="admin-page__scroll" scroll-y enhanced show-scrollbar="{{false}}">
    <view class="admin-page__form">
      <view class="admin-page__field" wx:if="{{isEdit}}">
        <view class="admin-page__label">部门编码</view>
        <view class="admin-page__readonly">{{deptCode}}</view>
      </view>
      <view class="admin-page__field" wx:else>
        <view class="admin-page__label">部门编码</view>
        <t-input
          layout="vertical"
          borderless
          placeholder="如 RD_DEPT"
          value="{{deptCode}}"
          status="{{fieldErrors.deptCode ? 'error' : 'default'}}"
          tips="{{fieldErrors.deptCode}}"
          t-class="admin-page__form-input"
          bind:change="onDeptCodeInput"
        />
      </view>

      <view class="admin-page__field">
        <view class="admin-page__label">部门名称</view>
        <t-input
          layout="vertical"
          borderless
          placeholder="请输入部门名称"
          value="{{deptName}}"
          status="{{fieldErrors.deptName ? 'error' : 'default'}}"
          tips="{{fieldErrors.deptName}}"
          t-class="admin-page__form-input"
          bind:change="onDeptNameInput"
        />
      </view>

      <view class="admin-page__field">
        <view class="admin-page__label">上级部门</view>
        <view class="admin-page__picker-trigger" bind:tap="onOpenParentPicker">
          <t-input
            layout="vertical"
            borderless
            readonly
            value="{{parentText}}"
            placeholder="请选择上级部门"
            suffix="›"
            t-class="admin-page__form-input"
          />
        </view>
      </view>

      <view class="admin-page__field" wx:if="{{isEdit}}">
        <view class="admin-page__label">状态</view>
        <view class="admin-page__picker-trigger" bind:tap="onOpenStatusPicker">
          <t-input
            layout="vertical"
            borderless
            readonly
            value="{{statusText}}"
            suffix="›"
            t-class="admin-page__form-input"
          />
        </view>
      </view>
    </view>
  </scroll-view>

  <view class="admin-page__footer admin-page__footer--fixed">
    <button class="admin-page__btn admin-page__btn--primary" disabled="{{submitting}}" bind:tap="onSubmit">
      <view class="btn-loading" wx:if="{{submitting}}">
        <t-loading theme="circular" size="40rpx" inherit-color />
      </view>
      <block wx:else>保存</block>
    </button>
  </view>

  <t-picker
    visible="{{parentVisible}}"
    value="{{parentPickerValue}}"
    title="选择上级部门"
    bindconfirm="onParentConfirm"
    bindcancel="onParentCancel"
  >
    <t-picker-item options="{{parentPickerOptions}}"></t-picker-item>
  </t-picker>

  <t-picker
    visible="{{statusVisible}}"
    value="{{statusPickerValue}}"
    title="选择状态"
    bindconfirm="onStatusConfirm"
    bindcancel="onStatusCancel"
  >
    <t-picker-item options="{{statusPickerOptions}}"></t-picker-item>
  </t-picker>

  <t-toast id="t-toast" />
</view>
